<p id="notice"><%= notice %></p>

<nav class="for_chart">
  <%= link_to "By days", products_path(by: "days")%>
  |
  <%= link_to "By weeks", products_path(by: "weeks")%>
  |
  <%= link_to "By months", products_path(by: "months")%>
  |
  <%= link_to "By quarters", products_path(by: "quarters")%>
</nav>
<%= content_tag :div, "", id: "orders_chart", data: {orders: chart_by(params[:by])} %>
<div id="annual"></div>

<h1>Listing Products</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Desc</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>
        <td><%= product.desc %></td>
        <td><%= product.price %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product', new_product_path %>
